import { solution } from './solution';

describe('[Problem Name]', () => {
  it('should handle example 1', () => {
    const input = null; // TODO: Add input
    const expected = null; // TODO: Add expected output
    expect(solution(input)).toEqual(expected);
  });

  it('should handle example 2', () => {
    const input = null; // TODO: Add input
    const expected = null; // TODO: Add expected output
    expect(solution(input)).toEqual(expected);
  });

  it('should handle edge case: empty input', () => {
    // TODO: Add test
  });

  it('should handle edge case: single element', () => {
    // TODO: Add test
  });

  it('should handle edge case: large input', () => {
    // TODO: Add test
  });
});
